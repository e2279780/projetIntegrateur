/**
 * Contexte utilisateur pour BiblioConnect
 * Séparé dans son propre fichier pour compatibilité Fast Refresh
 */

import { createContext } from 'react';

export const UserContext = createContext();
